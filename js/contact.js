const b=document.querySelector('.menu-toggle'),n=document.querySelector('.nav-links');b?.addEventListener('click',()=>n?.classList.toggle('open'));document.querySelectorAll('.nav-links a').forEach(a=>a.addEventListener('click',()=>n?.classList.remove('open')));document.getElementById('year').textContent=new Date().getFullYear();
const f=document.getElementById('contact-form'),s=document.getElementById('form-status');
f?.addEventListener('submit',e=>{e.preventDefault();const name=document.getElementById('name'),email=document.getElementById('email'),msg=document.getElementById('message');let ok=true;const err=(el,m)=>{el.parentElement.nextElementSibling.textContent=m;if(m)ok=false};err(name,name.value.trim().length<2?'Nom invalide':'');err(email,/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email.value)?'':'Email invalide');err(msg,msg.value.trim().length<10?'10 caractères minimum':'');s.textContent=ok?'Message prêt à être envoyé ✅':'Merci de corriger les erreurs';if(ok)f.reset();});
